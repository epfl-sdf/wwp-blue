---
- name: "PersistentVolume"
  openshift:
    as_admin: true
    state: "{{state}}"
    kind: PersistentVolume
    name: "nfs-pv-wp2"
    namespace: "myproject"
    apiVersion: v1
    metadata:
      name: nfs-pv-wp2
      labels:
          id: wp2
    spec:
      capacity:
        storage: 1Gi
      accessModes:
        - ReadWriteMany
      persistentVolumeReclaimPolicy: Retain
      nfs:
        path: /wp/wp2
        server: "{{nfs_ip}}"
        readOnly: false
  tags: pv
