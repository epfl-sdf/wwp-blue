---
- name: "PersistentVolumeClaim"
  openshift:
    as_admin: true
    state: "{{state}}"
    kind: PersistentVolumeClaim
    name: "nfs-pvc-wp2"
    namespace: "myproject"
    apiVersion: v1
    metadata:
      name: nfs-pvc-wp2
    spec:
      accessModes:
      - ReadWriteMany
      resources:
         requests:
           storage: 1Gi
      selector:
          matchLabels:
              id: wp2
  tags: pvc