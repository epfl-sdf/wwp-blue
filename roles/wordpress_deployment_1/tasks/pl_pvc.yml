---
- name: "PersistentVolumeClaim"
  openshift:
    as_admin: true
    state: "{{state}}"
    kind: PersistentVolumeClaim
    name: "nfs-pvc-wp1"
    namespace: "myproject"
    apiVersion: v1
    metadata:
      name: nfs-pvc-wp1
    spec:
      accessModes:
      - ReadWriteMany
      resources:
         requests:
           storage: 1Gi
      selector:
          matchLabels:
              id: wp1
  tags: pvc