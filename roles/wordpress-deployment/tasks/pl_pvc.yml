---
- name: "PersistentVolumeClaim"
  openshift:
    as_admin: true
    state: "{{state}}"
    kind: PersistentVolumeClaim
    name: "nfs-pvc"
    namespace: "myproject"
    apiVersion: v1
    metadata:
      name: nfs-pvc
    spec:
      accessModes:
      - ReadWriteMany
      resources:
         requests:
           storage: 1Gi
      selector:
          matchLabels:
              id: bat
  tags: pvc